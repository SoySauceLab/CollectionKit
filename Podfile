
use_frameworks!

target "CollectionKit" do

	target "CollectionKitTests" do
		inherit! :search_paths

		pod 'Quick'
		pod 'Nimble'
	end

	target "CollectionKitExample" do
		pod "CollectionKit/WobbleAnimator", :path => './'
	end
end
